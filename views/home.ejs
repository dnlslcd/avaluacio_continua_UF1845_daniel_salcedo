<!DOCTYPE html>
<html lang="en">
<head>
    <title>Homepage</title>
    <%- include('partials/head') %>
</head>
<body>
    <%- include('partials/header') %>
    <h1>Welcome to the Tribook Homepage</h1>

    <!-- BUSCAR apt
     1. Añadir formulario para buscar por precio máximo. Valor del atributo method?-> GET. Valor del atributo action? -> "/search" -->
        <form class="container" action="/search">
            <fieldset>
                <label for="maxPrice">Max. price per night</label>
                <input type="number" name="maxPrice" id="maxPrice" min="0" max="1000000" value="0">
            </fieldset>

    <main class="container apartments">
        <% for (const ap of apartments) { %>
        <a href="/apartment/<%= ap._id %>">
        <article class="apartment">
            <header>
                <%= ap.title %>
            </header>
            <div>
                <img src="<%= ap.mainPhoto %>" alt="">
            </div>
            <footer>
                <%= ap.price %> €/night
                <div>
                    <%= ap.size %> sqm
                </div>
            </footer>
        </article>
         </a>
    <% } %>
    </main>
    <%- include('partials/footer') %>
</body>
</html>